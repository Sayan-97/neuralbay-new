service : {
  addModel: (text, text, text) -> (text);
  getModels: () -> (vec record {
                          text;
                          text;
                          text;
                        });
}
